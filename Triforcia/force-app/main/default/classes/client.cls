public class client {
public string name = 'Gulchin';
    public integer age = 35;

    public void clientDataMethod()
    {
      string agetoText = string.valueof(age);
      system.debug('The name of the client is '+name+ ' and the age is '+age);
    
}
}